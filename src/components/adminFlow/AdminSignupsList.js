import React, { Component } from 'react';
import { Text } from 'react-native';

class AdminSignupsList extends Component {
    render() {
        return <Text>{this.props.titleKey}</Text>;
    }
}

export default AdminSignupsList;
